/* -------- LOAD LOADERS -------- */

if (window.hiddenDefine) {
  window.define = window.hiddenDefine
}

if (window.hiddenExports) {
  window.exports = window.hiddenExports
}

/* -------- END LOAD LOADERS -------- */
