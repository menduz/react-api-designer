/* -------- STORE LOADERS -------- */

if (window.define) {
  window.hiddenDefine = window.define
  window.define = undefined
}

if (window.exports) {
  window.hiddenExports = window.exports
  window.exports = undefined
}

/* -------- END STORE LOADERS -------- */
